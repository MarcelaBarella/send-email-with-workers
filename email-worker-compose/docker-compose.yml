version: '3'
volumes:
  dates:
services:
  db:
    image: postgres:9.6
    volumes:
      # Data volume
      - dates:/var/lib/postgressql/data
      # Scripts
      - ./scripts:/scripts
      - ./scripts/init.sql:/docker-entrypoint-initdb.d/init.sql